/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Sprint 1 local dev scaffold: no image domains / rewrites configured yet.
  // Frontend Developer: extend as pages need (e.g. MapLibre tile sources).
};

module.exports = nextConfig;
