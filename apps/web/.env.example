# Copy to apps/web/.env.local if you run `npm run dev` directly on your host
# (outside docker-compose). When running via `docker-compose up`,
# infra/docker-compose.yml already injects NEXT_PUBLIC_API_URL for you.

NEXT_PUBLIC_API_URL=http://localhost:8000
